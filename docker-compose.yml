version: '3.8'

services:
  # Backend + Frontend (usando MongoDB Atlas externo)
  app:
    build: .
    container_name: telemedicina-app
    restart: always
    ports:
      - "8080:5000"  # Backend en puerto 8080 externo
      - "3000:3000"  # Frontend en puerto 3000
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGODB_URI=mongodb+srv://fm159851_db_user:YljK3osUUHZGCgal@medconnect.mcj9hxa.mongodb.net/telemedicina?retryWrites=true&w=majority
      - JWT_SECRET=telemedicina_jwt_secret_2026_fm159851
      - CORS_ORIGIN=*
    volumes:
      - ./uploads:/app/uploads
